{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --dotenv=development",
    "generate": "nuxt generate --dotenv=production",
    "deploy": "rmdir /s .\\docs && move .\\dist .\\docs && copy nul .\\docs\\.nojekyll",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.7.4"
  }
}
